# Dependency artifacts
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Claude Code per-machine local settings (permission allowlist etc. — not shared)
.claude/settings.local.json

# Build output (none currently, but just in case)
dist/
build/
.gradle/

# Android local config (machine-specific)
local.properties

# Android signing keys — NEVER commit these
*.jks
*.keystore

# Python tooling
__pycache__/
*.pyc
